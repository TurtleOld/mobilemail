name: Android CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Prepare CI files
        run: |
          mkdir -p .keystore app

          keytool -genkeypair \
            -alias ci \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storetype JKS \
            -keystore .keystore/release.jks \
            -storepass android \
            -keypass android \
            -dname "CN=CI,O=MobileMail,C=US"

          cat > app/google-services.json <<'EOF'
          {
            "project_info": {
              "project_number": "1234567890",
              "project_id": "mobilemail-ci",
              "storage_bucket": "mobilemail-ci.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:1234567890:android:abcdef123456",
                  "android_client_info": {
                    "package_name": "com.mobilemail"
                  }
                },
                "api_key": [
                  {
                    "current_key": "fake-ci-key"
                  }
                ]
              }
            ],
            "configuration_version": "1"
          }
          EOF

      - name: Run Android checks
        env:
          KEYSTORE_PASSWORD: android
          KEY_ALIAS: ci
          KEY_PASSWORD: android
          NTFY_URL: https://ntfy.sh
          NTFY_TOKEN: ci
          NTFY_TOPIC_PATTERN: mobilemail/{user}
        run: |
          chmod +x ./gradlew
          ./gradlew --no-daemon --build-cache :app:lintDebug :app:testDebugUnitTest :app:assembleDebug

  instrumentation:
    runs-on: macos-13
    needs: checks

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Prepare CI files
        run: |
          mkdir -p .keystore app

          keytool -genkeypair \
            -alias ci \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -storetype JKS \
            -keystore .keystore/release.jks \
            -storepass android \
            -keypass android \
            -dname "CN=CI,O=MobileMail,C=US"

          cat > app/google-services.json <<'EOF'
          {
            "project_info": {
              "project_number": "1234567890",
              "project_id": "mobilemail-ci",
              "storage_bucket": "mobilemail-ci.appspot.com"
            },
            "client": [
              {
                "client_info": {
                  "mobilesdk_app_id": "1:1234567890:android:abcdef123456",
                  "android_client_info": {
                    "package_name": "com.mobilemail"
                  }
                },
                "api_key": [
                  {
                    "current_key": "fake-ci-key"
                  }
                ]
              }
            ],
            "configuration_version": "1"
          }
          EOF

      - name: Run connected instrumentation tests
        uses: reactivecircus/android-emulator-runner@v2
        env:
          KEYSTORE_PASSWORD: android
          KEY_ALIAS: ci
          KEY_PASSWORD: android
          NTFY_URL: https://ntfy.sh
          NTFY_TOKEN: ci
          NTFY_TOPIC_PATTERN: mobilemail/{user}
        with:
          api-level: 30
          target: google_apis
          arch: x86_64
          emulator-boot-timeout: 900
          disable-animations: true
          emulator-options: -no-window -no-snapshot -no-snapshot-save -wipe-data -noaudio -no-boot-anim -gpu swiftshader_indirect -camera-back none -camera-front none -no-metrics
          script: |
            chmod +x ./gradlew
            ./gradlew --no-daemon --build-cache :app:connectedDebugAndroidTest
